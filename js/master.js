// Runtime data
let runtimeData = {
    weekOffset:0
}

// Initialization
updateStoredData() // Begin updating local data

updateCalendar() // for testing only


// Debugging functions
function clearAllData() {
    localStorage.clear()
}